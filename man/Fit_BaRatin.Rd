% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BaRatin_tools.R
\name{Fit_BaRatin}
\alias{Fit_BaRatin}
\title{BaRatin}
\usage{
Fit_BaRatin(
  x,
  y,
  time = 1:NROW(y),
  uX = 0 * x,
  uY = 0 * y,
  flavor = c("BaRatin", "BaRatinBAC"),
  controlMatrix = matrix(1),
  priors = get3FlatPriors(x, y),
  mcmc_options = RBaM::mcmcOptions(),
  mcmc_cooking = RBaM::mcmcCooking(),
  remnant = list(RBaM::remnantErrorModel()),
  temp.folder = file.path(tempdir(), "BaRatin")
)
}
\arguments{
\item{x}{real vector, stage}

\item{y}{real vector, discharge}

\item{time}{vector (numeric or date), time}

\item{uX}{real vector, stage uncertainty (as a standard deviation)}

\item{uY}{real vector, discharge uncertainty (as a standard deviation)}

\item{flavor}{string, BaRatin flavor}

\item{controlMatrix}{square matrix, control matrix}

\item{priors}{list, list of RBaM::parameter objects containing information for each
parameter of the rating curve. Length 3*NROW(controlMatrix)}

\item{mcmc_options}{mcmcOptions object, see ?RBaM::mcmcOptions}

\item{mcmc_cooking}{mcmcCooking object, see ?RBaM::mcmcCooking}

\item{remnant}{list of one remnantErrorModel object, see ?RBaM::remnantErrorModel}

\item{temp.folder}{string, directory where config and result files are stored.}
}
\value{
An object of class \code{\link[=fittedModel]{fittedModel()}}, containing the following fields:
\enumerate{
\item data: data frame, column-binding all the arguments of the function except parameters
\item parameters: numeric vector, fitted parameters
}
}
\description{
Fit a rating curve with BaRatin
}
\examples{
f=Fit_BaRatin(x=ArdecheRiverGaugings$H,y=ArdecheRiverGaugings$Q,
              uY=ArdecheRiverGaugings$uQ,time=ArdecheRiverGaugings$Date,
              # MCMC options are modified to speed-up example.
              # Using default MCMC options is safer and is recommended.
              mcmc_options=mcmcOptions(nAdapt=50,nCycles=20))
if(!is.null(f)){
  plot(f$data$x,f$data$y);points(f$data$x,f$data$ysim,col='red')
  plot(f$data$time,f$data$res)
}
}
