% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BaRatin_tools.R
\name{Segmentation_BaRatin}
\alias{Segmentation_BaRatin}
\title{BaRatin-based segmentation}
\usage{
Segmentation_BaRatin(
  H,
  Q,
  time = 1:NROW(Q),
  uH = 0 * H,
  uQ = 0 * Q,
  flavor = c("BaRatin", "BaRatinBAC"),
  controlMatrix = matrix(1),
  priors = get3FlatPriors(H, Q),
  nSmax = 2,
  doQuickApprox = TRUE,
  nMin = ifelse(doQuickApprox, 3, 1),
  mcmc_options = RBaM::mcmcOptions(),
  mcmc_cooking = RBaM::mcmcCooking(),
  remnant = list(RBaM::remnantErrorModel()),
  temp.folder = file.path(tempdir(), "BaRatin")
)
}
\arguments{
\item{H}{real vector, stage}

\item{Q}{real vector, discharge}

\item{time}{vector (numeric or date), time}

\item{uH}{real vector, stage uncertainty (as a standard deviation)}

\item{uQ}{real vector, discharge uncertainty (as a standard deviation)}

\item{flavor}{string, BaRatin flavor}

\item{controlMatrix}{square matrix, control matrix}

\item{priors}{list, list of RBaM::parameter objects containing information for each
parameter of the rating curve. Length 3*NROW(controlMatrix)}

\item{nSmax}{integer, maximum number of segments to assess}

\item{doQuickApprox}{logical, use quick approximation? see ?Segmentation_quickApprox}

\item{nMin}{integer, minimum number of observations by segment}

\item{mcmc_options}{mcmcOptions object, options used to generate MCMC samples (e.g. size and adaption tunings), see ?RBaM::mcmcOptions.
Only used when doQuickApprox=FALSE.}

\item{mcmc_cooking}{mcmcCooking object, options used to post-process MCMC samples (e.g. burn and slice), see ?RBaM::mcmcCooking.
Only used when doQuickApprox=FALSE.}

\item{remnant}{list of one remnantErrorModel object, see ?RBaM::remnantErrorModel}

\item{temp.folder}{directory, temporary directory to write BaM computations.
Only used when doQuickApprox=FALSE.}
}
\value{
An object of class \code{\link[=fittedModel]{fittedModel()}}, containing the following fields:
\enumerate{
\item data: data frame, column-binding all the arguments of the function except parameters
\item parameters: numeric vector, fitted parameters
}
}
\description{
Segmentation based on gaugings and a recursive estimation of BaRatin rating curves.
This function is just a wrapper around \code{\link[=Segmentation_RecursiveModeling]{Segmentation_RecursiveModeling()}}.
}
\examples{
sg=Segmentation_BaRatin(H=ArdecheRiverGaugings$H,Q=ArdecheRiverGaugings$Q,
                        uQ=ArdecheRiverGaugings$uQ,time=ArdecheRiverGaugings$Date,
                        # MCMC options are modified to speed-up example.
                        # Using default MCMC options is safer and is recommended.
                        mcmc_options=mcmcOptions(nAdapt=30,nCycles=20))
if(!is.null(sg)){
  plot(sg,dataPlotType='ty')
  patchwork::wrap_plots(plot(sg,type='fits'))
}
}
