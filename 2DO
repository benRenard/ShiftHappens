utilities_private: OK

utilities_public: OK

data: OK

Segmentation: 
[DONE] consider creating a class for the results
[DONE] Consider removing unnecesaary fields, e.g. plot, segments, data.p
[DONE] rename Recursive_Segmentation to Segmentation_Recursive

[DONE] Everywhere: replace MCMC options by proper RBaM::mcmcOptions and RBaM::mcmcCooking objects

[DONE] Plots: a lot of simplification/factorization is possible, by taking advantage of ggplots + functionalities (e.g. do not create a new function to change titles, +labs() will work). Stick to context-independent plots here. If need be, specialized plots should be in *_tools modules.

[DONE] fitRC: should become fitModel (pIN-1OUT), and be context-independent. Mostly involves changing functions arguments (e.g. X/Y rather than H/Q). Also create a class for results

[DONE] modelandsegment_gaugings: should become recursiveModeling, and be context-independent. Or maybe need to stick to recursiveCurveFitting, i.e. 1IN-1OUT model (or maybe pIN-1OUT) ? In which case we may have curve_tools (which would include fitCurve)? -> make a context-independent recursiveModeling function, linked to any fitModel function.

[DONE] estimation_recessions: should not exist, functions should be part of recessions_tools

[DONE] FIX BUG IN HANDLING OF origin.date IN RECURSIVE SEGMENTATION (and RECURSIVE MODELING)

[DONE] Define a plot method for fitted Model class ?

[DONE] Needs a BaRatin_tools module? or can it stay part of curve_tools? -> No, make a BaRatin_tools, including in particular the fitRC_BaRatinXXX functions -> Not sure about that. At least fitRC_BaRatinXXX should rather go into fitModel. If there are other BaRatin utilities than this module may be worth it. -> No, finally fit_BaRatin goes into BaRatin_tools

[DONE] Catalog: OK, but review link with equations_RC_recessions - has been removed

[DONE] equations_RC_recessions: should not exist? or it should at least be named equations_curve_recessions, but most likely it should be part of specific *_tools modules (or directly in Catalog?) - just a list that is part of recession_tools

[DONE] modelandsegment_recessions: should not exist. Instead, create file recessions_tools, that includes recession model fitting, plots, and possibly a wrapping function to do recession - fit - segment in one go.


